# -*- mode: snippet -*-
# name: jsintegration
# key: jsintegration
# --
require 'test_helper'
require_relative 'givens'

class ${1:`(classify-buffer-name)`} < ActionDispatch::IntegrationTest
  include Givens

  if ENV['SKIP_BROWSER_TESTS'] != '1'
    setup do
      stub_user_methods
      stub_city_callbacks
      Capybara.current_driver = :selenium
      WebMock.disable_net_connect!(allow_localhost: true)
      $0
    end
  end
end
